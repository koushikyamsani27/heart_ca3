apiVersion: v1
kind: Service
metadata:
  name: diabetes-prediction-service
spec:
  type: NodePort
  selector:
    app: diabetes-prediction
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30007